# Contact form backend. The site is static (GitHub Pages), so form submissions
# POST to this external endpoint (Formspree, Cloudflare Worker, or a future API).
# When unset, the form falls back to opening a pre-filled mailto: draft.
# Set it as a repository Actions secret/variable for production builds.
VITE_CONTACT_ENDPOINT=
